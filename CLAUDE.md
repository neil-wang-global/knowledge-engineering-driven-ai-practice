# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository State

This repository is currently empty except for Git metadata and this guidance file. There is no README, source tree, package manifest, build configuration, test configuration, Cursor rules, or Copilot instructions present yet.

## Git Remote

- `origin`: `https://github.com/neil-wang-global/knowledge-engineering-driven-ai-practice.git`

## Commands

No project-specific build, lint, test, or development commands are defined yet. After adding a language/runtime manifest or build tool configuration, update this section with the exact commands used by the project.

Useful repository commands in the current state:

```bash
git status --short --branch
git remote -v
```

## Architecture

No application architecture exists yet because the repository does not currently contain source code. When code is added, update this file with the high-level module boundaries, entry points, runtime dependencies, and any cross-file design relationships that are not obvious from the directory layout.
